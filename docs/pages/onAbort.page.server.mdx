import { Link } from '@brillout/docpress'

Environment: Browser.

The `onAbort()` hook is usually used for implementing `getContext()` wrappers, see <Link href="/permissions#getcontext-wrapping" doNotInferSectionTitle={true} />.

The `onAbort()` hook is called whenever a telefunction remote call failed because the telefunction did a <Link text={<code>throw Abort()</code>} href="/Abort" />.
