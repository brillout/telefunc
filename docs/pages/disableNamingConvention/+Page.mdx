import { Link } from '@brillout/docpress'
import { ConfigWhereServer, EventBasedRecommendation } from '../../components'

**Environment**: server.

Opt out of the <Link href="/event-based#naming-convention">naming convention for event-based telefunctions</Link> (removes all related warnings).

```ts
// Environment: server

import { config } from 'telefunc'

config.disableNamingConvention = true
```

<EventBasedRecommendation />

<ConfigWhereServer />


## See also

- <Link href="/event-based" />
- <Link href="/event-based#opt-out" />
