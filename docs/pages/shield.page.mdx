import { Link } from 'libframe-docs/components'

As we have seen in the <Link text="Telefunc Tour" href="/tour" />,
telefunctions are public can be called by anyone.
We use `shield()` to ensure the type of telefunction arguments.


