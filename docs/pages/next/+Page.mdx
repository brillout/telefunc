import { Link } from '@brillout/docpress'
import { MostlyMutations } from '../components'

## Example

- <Link href="/examples/next" />

## Initial Data

<MostlyMutations
  toolName="Next.js"
  builtInMechanism={
    <p>
      For example, we can use Next.js's <code>getServerSideProps</code> to fetch initial data directly from a database,
      see{' '}
      <a href="https://nextjs.org/docs/basic-features/data-fetching/get-server-side-props">
        Next.js Docs &gt; Data Fetching &gt; getServerSideProps
      </a>
      {'.'}
    </p>
  }
/>
