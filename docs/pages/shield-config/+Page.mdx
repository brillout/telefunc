import { Link } from '@brillout/docpress'
import { ConfigWhereServer } from '../../components'

**Environment**: server.

Whether to generate shield in development environment. Default is `false`.

```js
// Environment: server

import { config } from 'telefunc'

config.shield.dev = true
```

<ConfigWhereServer />
