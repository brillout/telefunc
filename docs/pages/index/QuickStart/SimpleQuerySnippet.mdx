```tsx
// UpdateTodo.tsx
// Environment: client

import { onUpdateTodoText } from './UpdateTodo.telefunc.ts'

export function CreateTodo() {
	async function onClick(form: ChangeEvent) {
		await onNewTodo(form.input.value ?? '')
	}
	
	return (
		<form>
			<input input="text" />
			<button onClick={onClick}>
				Add To-Do
			</button>
		</form>
	)
}
```
