import { Link } from '@brillout/docpress'
import PermissionsBasics from '../../../components/PermissionsBasics.mdx'

<PermissionsBasics />

We can also define wrappers around `getContext()` to implement a permission once and re-use it, see <Link href="/permissions#getcontext-wrapping" />.
