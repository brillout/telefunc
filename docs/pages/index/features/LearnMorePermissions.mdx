import { Link } from '@brillout/docpress'
import PermissionsBasics from '../../../components/PermissionsBasics.mdx'

<PermissionsBasics />

We can also define wrappers around `getContext()` to implement permissions once and re-use them &mdash; see <Link href="/permissions" />.
