import { Link } from '@brillout/docpress'
import { ConfigWhereClient } from '../../components'

**Environment**: client.

Send additional HTTP headers to be sent along Telefunc HTTP requests.

Usually used for sending authentication headers.

```ts
// Environment: client

import { config } from 'telefunc/client'

config.httpHeaders = {
  Authorization: `Bearer ${token}`
}
```

<ConfigWhereClient />


## See also

- <Link href="/fetch" />
- <Link href="/permissions" />
- [#167 - Dynamic `httpHeaders`?](https://github.com/brillout/telefunc/issues/167)
