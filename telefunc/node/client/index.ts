// Dummy emtpy file. (Some frameworks have no clean cut speratation between client and server code; sometimes the client code wrongfully tries to transpile server code, and vice versa.)

export const config = {}
