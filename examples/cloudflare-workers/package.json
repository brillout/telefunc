{
  "scripts": {
    "dev": "node ./devServer",
    "build": "vite build && vite build --ssr --emptyOutDir false",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler publish"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.4.0",
    "express": "^4.17.2",
    "telefunc": "0.2.12",
    "vite": "6.3.0",
    "wrangler": "^4.28.0"
  }
}
